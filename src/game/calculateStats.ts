import type{DinosaurConfig}from'./dinosaurTypes';export type DinoStats={speed:number;strength:number;balance:number;swimming:number;stamina:number};const c=(n:number)=>Math.max(1,Math.min(5,n));export function calculateStats(d:DinosaurConfig):DinoStats{let speed=3,strength=3,balance=3,swimming=2,stamina=3;if(d.legs==='Long'){speed++;balance--}if(d.legs==='Short'){balance++;speed--}if(d.body==='Big'){strength++;speed--;stamina++}if(d.body==='Small'){speed++;strength--}if(d.tail==='Giant'){balance+=2;speed--}if(d.tail==='Long')balance++;if(d.tail==='Short')speed++;if(d.head==='Big Chomper')strength++;if(d.head==='Long Snout')swimming++;if(d.feature==='Plates'){balance++;stamina++}if(d.feature==='Horns')strength++;if(d.feature==='Spikes')stamina++;return{speed:c(speed),strength:c(strength),balance:c(balance),swimming:c(swimming),stamina:c(stamina)}}
