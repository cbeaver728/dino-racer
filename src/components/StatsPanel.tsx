import type{DinosaurConfig}from'../game/dinosaurTypes';import{calculateStats}from'../game/calculateStats';export function StatsPanel({config}:{config:DinosaurConfig}){return <section className="stats"><h2>🏁 Future Race Stats</h2>{Object.entries(calculateStats(config)).map(([k,v])=><div className="stat" key={k}><span>{k}</span><span aria-label={`${v} out of 5`}>{[0,1,2,3,4].map(i=><i className={i<v?'on':''} key={i}/>)}</span></div>)}</section>}
