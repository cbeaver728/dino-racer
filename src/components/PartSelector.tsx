type P<T extends string>={label:string;icon:string;value:T;options:readonly T[];onChange:(v:T)=>void};export function PartSelector<T extends string>({label,icon,value,options,onChange}:P<T>){const go=(n:number)=>onChange(options[(options.indexOf(value)+n+options.length)%options.length]);return <div className="selector"><span>{icon} {label}</span><div><button aria-label={`Previous ${label}`} onClick={()=>go(-1)}>◀</button><strong>{value}</strong><button aria-label={`Next ${label}`} onClick={()=>go(1)}>▶</button></div></div>}
